apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: traefik-data-pvc
  namespace: traefik
spec:
  accessModes:
    - ReadWriteOnce
  storageClass: local-path
  resources:
    requests:
      storage: 1Gi