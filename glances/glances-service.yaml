apiVersion: v1
kind: Service
metadata:
  name: glances
  namespace: monitoring
  labels:
    app.kubernetes.io/name: glances
spec:
  type: ClusterIP
  ports:
    - name: "61208"
      port: 61208
      targetPort: 61208
  selector:
    app.kubernetes.io/name: glances