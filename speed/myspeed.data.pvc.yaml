apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: myspeed-data-pvc
  namespace: speed
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi